import { Plugin } from "unified";
import * as Ast from "../../unified-latex-types";
declare type ComplierOptions = {
    pretty?: boolean;
    printWidth?: number;
    useTabs?: boolean;
} | undefined;
/**
 * Unified complier plugin that prints a LaTeX AST as a string.
 */
export declare const unifiedLatexStringComplier: Plugin<ComplierOptions[], Ast.Root, string>;
export {};
