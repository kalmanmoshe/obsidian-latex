/**
 * Convert `tag` into an escaped macro name.
 */
export declare function tagName(tag: string): string;
/**
 * Convert `attribute` into an escaped macro name.
 */
export declare function attributeName(attribute: string): string;
/**
 * Extract a tag name from an escaped macro name.
 */
export declare function getTagNameFromString(tagName: string): string;
/**
 * Extract an attribute name from an escaped macro name.
 */
export declare function getAttributeNameFromString(tagName: string): string;
