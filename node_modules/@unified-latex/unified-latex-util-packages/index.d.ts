import * as Ast from '@unified-latex/unified-latex-types';

/**
 * List all packages referenced via `\includepackage{...}` or `\RequirePackage{...}`
 *
 * @param {Ast.Ast} tree
 * @returns {string[]}
 */
export declare function listPackages(tree: Ast.Ast): Ast.String[];

export { }
